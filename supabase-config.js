// Supabase project connection details.
//
// It is safe for this key to be visible in the app's public source code —
// it's the "publishable" key, meant to be used from the browser. It can
// only do what the Row Level Security policies on the project allow (see
// the SQL setup script); it cannot bypass them or read/write anything the
// policies don't permit. Never put the "secret" (service_role) key here or
// anywhere else in this app.
window.SUPABASE_URL = 'https://aleadvxqoqmfxtqpiikd.supabase.co';
window.SUPABASE_PUBLISHABLE_KEY = 'sb_publishable_VMIZZYrdVuf_S7pknl7grQ_YnVzAy7R';
